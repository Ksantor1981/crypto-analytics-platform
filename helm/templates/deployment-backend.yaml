apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          ports:
            - containerPort: {{ .Values.backend.port }}
          env:
            - name: DATABASE_URL
              value: {{ required "backend.env.DATABASE_URL is required (see helm/README.md)" .Values.backend.env.DATABASE_URL | quote }}
            - name: REDIS_URL
              value: {{ required "backend.env.REDIS_URL is required (include redis password in URL)" .Values.backend.env.REDIS_URL | quote }}
            - name: ENVIRONMENT
              value: {{ .Values.backend.env.ENVIRONMENT | quote }}
            - name: SECRET_KEY
              value: {{ required "backend.env.SECRET_KEY is required" .Values.backend.env.SECRET_KEY | quote }} 